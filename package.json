{
  "name": "@friendly/mautic-content-block-bundle-assets",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build":     "node build.js --minify",
    "build:dev": "node build.js",
    "watch":     "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
